\documentclass{article}
\usepackage{graphicx} % Required for inserting images


\usepackage{amsfonts}
\usepackage{amsthm}
\usepackage{amssymb}
\usepackage{amsmath}
\usepackage{fullpage}
\usepackage{upgreek}
\usepackage{caption}
\usepackage{appendix}
\usepackage{bm}

\newcommand{\gd}{{\dot{\bm{\gamma}}}}

\title{On the Capabilities of Self-Similar Viscosity Models to Predict Properties of Carreau Boundary Layer Flows}
\author{W. Cade Reinberger, Nathaniel S. Barlow, and Steven J. Weinstein}
\date{\today}



\begin{document}



\maketitle

\begin{abstract}
   We consider the steady boundary layer flows induced by the relative motion of a flat wall and a non-Newtonian fluid under conditions where the wall is moving (Sakiadis flow) and stationary (Blasius flow). These flows admit self-similar ordinary differential equation (ODE) solutions for Newtonian or power-law viscosity models, but not for other generalized-Newtonian viscosity models that enable a smooth transition between Newtonian and power-law behaviors.  Since the rates of strain near the wall are high and those away from the wall and far downstream are small, location-dependent power law and Newtonian behaviors are often incurred in the same boundary layer flow.  Nevertheless, the power law model is often used to model non-Newtonian boundary layer flows because of its ease of use through its similarity solution.  Here, we compare flow-field predictions from self-similar boundary layer flows to those obtained using the Carreau viscosity model, and use wall shear predictions as a metric by which viscosity models can be compared.  To enable this comparison, we determine new similarity solutions to the Sakiadis boundary layer flow for both shear thickening and shear thinning fluids, and shed light on the mathematical structure of previously-identified discontinuous solutions found in the Blasius problem for shear thickening fluids. We show that the wall shears predicted using the Carreau model can be well-approximated by those from Newtonian and power law self-similar solutions at locations along the wall satisfying a delineating dimensionless criterion. This result holds for both the Sakiadis and Blasius flows for all shear-thinning and thickening fluid properties examined.
\end{abstract}

\section{Introduction}

\hspace{\parindent} A boundary layer describes a thin region adjacent to a wall where viscous effects are important in an otherwise inviscid flow field. The classical boundary layer flow was described by Blasius \cite{Blasius}, who in 1908 derived a similarity solution for the steady laminar flow of a Newtonian fluid over a stationary and flat semi-infinite wall. Later, Sakiadis \cite{Sakiadis1961a} extended this framework to model the case of a flat moving wall in an otherwise stationary fluid—a configuration with direct relevance to industrial processes such as liquid film coating. The ``Blasius” and ``Sakiadis” boundary layers are mathematically distinct; the nonlinear structure of the governing equations precludes a simple translation of reference frame that relates both problems via a single mathematical solution.

In both flows, the power of similarity solutions lies in reducing the governing partial differential equations in two dimensions to an ordinary differential equation (ODE) in a single similarity variable. These classical solutions have since been generalized to include fluids with more complex rheological behaviors.  The Ostwald de Waele power law model is the most commonly used due to its analytical simplicity, and is expressed as

\begin{equation}
    \mu = K \left|\gd\right|^{\alpha-1}. \label{power_law_constitutive_equation}
\end{equation}


\noindent In (\ref{power_law_constitutive_equation}), $\mu$ is the viscosity, $K$ is the consistency coefficient, $\alpha$ is the power law exponent, $\gd$ is the rate of strain tensor that depends on velocity gradients, and $|\gd|$ is its magnitude. The Blasius and Sakiadis flows of a power-law fluid were first examined by \cite{Acrivos1960} and \cite{Fox}, respectively. Since then, much literature has focused on the mathematical structure of these flows and their physical applicability \cite{Cortell, Takhar1991, And2007, Pant2004}. 

The power-law rheological model can, in general, model shear-thinning or shear-thickening behavior, but fails to capture Newtonian behavior at both low and high rates of strain characteristic of fluids encountered in practice (Figure 1). This is especially relevant to boundary layer flows, where rates of strain near the wall are often high and those away from the wall approach zero.  Furthermore, as the boundary layer grows downstream, the corresponding rates of strain in the boundary layer decrease, so the relative importance of various portions of a rheological profile to the overall flow field can change.  Thus, depending on rheology, a complete rheological profile may be accessed in a single boundary layer flow, and the extremities in the profile shown in Figure 1 may be important to the overall flow dynamics.

\begin{figure}
    \centering
    \includegraphics[width=.4\linewidth]{shear_thinning.png}
    \includegraphics[width=.4\linewidth]{shear_thickening.png}
    \caption{Typical rheological curves showing the dependence of viscosity, $\mu$, on the magnitude of the rate of strain tensor, $|\gd|$.  The power law region has slope $\alpha-1$ on a log-log plot: a) Shear thinning behavior; b) Shear-thickening behavior.}
    \label{fig:enter-label}
\end{figure}  

To address these deficiencies, more realistic generalized viscosity relations---such as the Carreau model---have been used in previous studies of Sakiadis and Blasius boundary layer flows \cite{PANTOKRATORAS, Griffiths}.  The Carreau model incorporates both shear-dependent power law behavior and its asymptotic Newtonian limits, providing a smooth transition between low-shear and high-shear regimes as shown in Figure 1; it can thus fully characterize many typical experimentally-measured viscosity curves. The Carreau model is expressed as 

\begin{equation}
    \mu = \mu_\infty + (\mu_0 - \mu_\infty)\left(1 + (\lambda|\gd|)^2\right)^{(\alpha-1)/2}
\end{equation} where $\mu_0$ is the Newtonian viscosity at low strain rate magnitudes ($|\gd| \ll 1/\lambda$), $\mu_\infty$ is the Newtonian viscosity at high strain rate magnitudes ($|\gd| \gg 1/\lambda$), and $\lambda$ is a relaxation time parameter.  In many flow fields achieved in practice, the high strain rate Newtonian plateau is not reached, and rheological data may be fit well by setting $\mu_\infty = 0$. Under these circumstances, the Carreau model becomes \begin{subequations}
\begin{equation}
    \mu = \mu_0\left(1 + (\lambda|\gd|)^2\right)^{(\alpha-1)/2}.
\end{equation} From this, it is easy to deduce low shear and power-law asymptotes shown in Figure 1 as
 \begin{equation} \mu \sim \mu_0 ~~~~(|\gd| \to 0) \hphantom{\sum\sum} \mu \sim \mu_0\lambda^{\alpha-1} |\gd|^{\alpha-1} ~~~~(|\gd| \to \infty). \end{equation}
The intersection between these asymptotes defines the critical rate of strain, $|\gd|_c$, for the onset of shear thinning as \begin{equation} |\gd|_c = \frac{1}{\lambda}. \label{power_law_carreau_limit}\end{equation} Thus, the parameter, $\lambda$, is seen to control the onset of shear thinning through (\ref{power_law_carreau_limit})  (see Figure 1), and the consistency coefficient, $K$, in the limiting power-law model may be expressed in terms of Carreau parameters by comparing (\ref{power_law_carreau_limit}) and (\ref{power_law_constitutive_equation}) as \begin{equation}
    K = \mu_0 \lambda^{\alpha-1}. \label{carreau_power_law_limit_constistency_parameter}
\end{equation} \end{subequations} The connection between the Carreau and power law models, then, is thus made clear via (\ref{carreau_power_law_limit_constistency_parameter}). Note that the power law model ((1) with (3d)) and the Carreau model ((2) or (3a)) reduce to a constant Newtonian viscosity for $\alpha=1$, i.e. \begin{equation}
    \mu = \mu_0 ~~~~\forall ~ |\gd|.
\end{equation}

The power-law model is often utilized in many fundamental research studies to examine the effects of shear-dependent viscosity—this is to examine phenomenologically how such effects can affect a flow, regardless of applicability in practice.  To date, the authors are unaware of a systematic study demonstrating the efficacy of the power law model in predicting boundary layer flow structure compared with more realistic viscosity models. One feature of such models---such as the Carreau model examined in this paper---is that they do not admit similarity solutions for the Blasius and Sakiadis flows, and this complicates both analysis and computation.  One aim of the present work is to assess the extent to which the predictions of simpler, self-similar models provide adequate descriptions of Blasius and Sakiadis flows compared with those using the Carreau model.  To do so, we focus on wall shear as a metric of comparison, but also examine differences in velocity profiles that arise between these models.  

The power law model provides an incomplete description of fluid rheology (again, refer to Figure 1), and there are some notable changes in the mathematical structure of boundary layer flows that arise upon changing the value of the power law exponent, i.e. $\alpha$ in (1).  This change of structure is relevant to our comparative study of power law and Carreau model results, and we recount some of that literature here.  For Blasius boundary layers, it is well-known that an infinitely differentiable velocity profile is obtained for shear-thinning fluids ($\alpha < 1$), but the velocity profile loses smoothness for shear-thickening fluids ($\alpha>1$): the velocity profile has a discontinuity in the velocity field in its fourth derivative, although continuous in its third.  Denier and Dabrowski \cite{Dabrowski} indicate that this discontinuous solution structure arises from a breakdown in the boundary layer equations far from the wall when the viscosity is suitably small.  They demonstrate that a continuous solution can be obtained by introducing a “viscous diffusion layer” that incorporates viscous derivatives neglected in the boundary layer equation. In this paper, we demonstrate that the Carreau model—a viscosity model with built-in Newtonian limits—yields a continuous solution for all $\alpha$ without the need to invoke modifications to the boundary layer approximation itself.  

Previous work on the Sakiadis boundary layer using a power law model indicates that no solution exists for all shear-thickening fluids ($\alpha > 1$) and for some shear-thinning fluids ($\alpha < 1/2$) \cite{Fox, NonNewtonian}. These results are based on the fact that the dimensionless stream function, $f$, satisfies $\text{d}f/\text{d}\eta \to 0$ as $\eta \to \infty$ , where $\eta$ is the similarity variable (here corresponding to large distances from the wall). Previous work assumes an asymptotic form $f \sim C + h(\eta)$ where $C$ is a constant and $h(\eta) \ll C$ as $\eta \to \infty$. The claim of no solutions for $\alpha < 1/2$ is based on the fact that this assumption is not consistent for $\alpha < 1/2$.  However, in this paper we demonstrate that solutions do exist in the form $f \sim A(\eta + E)^p$, where $A$ and $E$ are constants with $0<p<1$.  Furthermore, solutions do exist for shear-thickening flows ($\alpha > 1$) with $f$ identically constant for $\eta$ sufficiently large, and these solutions are \textit{not} continuously differentiable in their fourth derivative; this structure is analogous to that found in the Blasius problem for a shear-thickening power law fluid \cite{Zhizhin1978-us, Pavlov1982-qh}. We show these additional boundary layer solutions for power-law fluids do yield wall-shear stress predictions that agree with those from the Carreau model at locations along the wall where significant shear-dependent behavior is invoked.  

The organization of the paper is as follows. In section 2, the formulations of the Sakiadis and Blasius problems are provided for Newtonian, Power Law, and Carreau model fluids. Although the Carreau constitutive law does not admit a similarity solution, we make use of previous literature \cite{Griffiths} to recover a one-parameter family of ODE solutions that describe the flow at each streamwise location along the wall. In section 3, we examine the new power-law solutions we have identified for the Sakiadis flow for $\alpha < 1/2$ and $\alpha > 1$. Section 4 presents results that show the wall shears predicted for a Carreau fluid are well-approximated by those predicted by a corresponding Newtonian or power-law similarity solutions depending on the regime of wall shear. Section 4 also provides comparison of velocity profiles between different viscosity models.  In section 5, we provide a general discussion on the suitability of self-similar solutions as a model for realistic Carreau behavior, and we provide conclusions in section 6.


\section{Formulation}

\subsection{Boundary Layer Equations}

\begin{figure}
    \centering
    \includegraphics[width=1\linewidth]{sak_stuff.pdf}
    \caption{Configurations for the Sakiadis and Blasius Flows}
    \label{flow_setups}
\end{figure}

We consider the steady boundary layer flow induced by the motion between a fluid and an infinite flat plate (shown in Figure 2). There are two configurations that we examine: the \textit{Sakiadis} boundary layer induced by a moving wall in an otherwise stationary fluid (Figure 2b), and the \textit{Blasius} boundary layer induced by a moving fluid over a stationary wall (Figure 2a). Figure 2 shows a schematic corresponding to both configurations showing the coordinate system and corresponding velocities. Here, the $x$ coordinate is oriented along the wall, the $y$-direction is perpendicular to the wall, and the flow is invariant in the $z$-direction (not shown---oriented out of Figure 2). Corresponding velocities in the $x$ and $y$ directions are $u_x$ and $u_y$ respectively.  In the Blasius configuration, $u_x=U$ is the velocity of the fluid away from the wall ($y \to \infty$); while in the Sakiadis problem, $u_x=U$ is the velocity of the wall ($y=0$).  In both problems, the pressure field is constant and plays no role in the flow dynamics.


In accordance with the boundary layer approximation, the flow in Figure 2 is essentially inviscid away from the wall, and the effects of viscosity are constrained near a thin region---a boundary layer---near the wall.  As such, velocity variations in the y direction are large compared with those in the $x$ direction, and viscous stresses across the boundary layer dominate.  As a result, the boundary layer form of the equations of motion becomes \cite{Schlichting}

\begin{subequations} \label{blsystem}
    \begin{align}
        &\frac{\partial u_x}{\partial x} + \frac{\partial u_y}{\partial y} = 0 \label{conteqinbl}\\ 
        & \rho \left\{ u_x \frac{\partial u_x}{\partial x} + u_y \frac{\partial u_x}{\partial y} \right\} = \frac{\partial \tau_{yx}}{\partial y} \label{blmomeq}
    \end{align} where the relevant component of the stress tensor is given as \begin{align}
        \tau_{yx} = \mu \frac{\partial u_x}{\partial y},~~~~\mu = \mu(|\gd|),~~~~|\gd| = \left|\frac{\partial u_x}{\partial y} \right|. \label{stress_tensor_component}
    \end{align} In (\ref{stress_tensor_component}), a generalized Newtonian form for viscosity $\mu$ has been assumed, as it is a function of the magnitude of the rate of strain as indicated. Consistent with the configurations shown in Figure \ref{flow_setups}, the boundary conditions are \begin{align}
        \text{Location} && \text{Blasius}~~~~~ && \text{Sakiadis}~~~~~ \nonumber \\
        y = 0 && u_x = 0 ~~~~ u_y = 0 && u_x = U~~~~u_y=0 \\
        y \to \infty && u_x \to U~~~~~ && u_x \to 0.~~~~~
    \end{align} \end{subequations}

In this paper, we examine specific forms of $\mu(|\gd|)$ in (\ref{stress_tensor_component}) for power-law (1), Carreau (2) (or (3a)), and Newtonian models (4).  Regardless of viscosity model, the system (\ref{blsystem}) can be expressed in terms of a streamfunction $\psi$, satisfying $u_x = \partial \psi/\partial y$ and $u_y = -\partial \psi/\partial x$. The equations are then typically further simplified via similarity transform or PDE solution methods as discussed below.

\subsection{Power Law and Newtonian Fluids}

For a power law fluid, the partial differential (PDE) system (5) admits a similarity solution using the substitutions \begin{subequations}
    \begin{equation}
        \eta = y\left(\dfrac{\rho U^{2-\alpha}}{Kx}\right)^{1/(\alpha+1)}
    \end{equation} 
    \begin{equation}
        f_p(\eta; \alpha) = \frac{\eta \psi}{Uy}
    \end{equation}
\end{subequations} that yields an ODE for $f_p$ given by \begin{subequations} \begin{align}
    & \alpha(\alpha+1)f_p''' + f_pf_p''|f_p''|^{1-\alpha} = 0 \label{power_law_ode_only} \\ &(\mathrm{Blasius})~~~~f_p(0; \alpha)=0~~~~f_p'(0; \alpha)=0~~~~f_p'(\infty; \alpha) \to 1  \\
    & (\mathrm{Sakiadis})~~~~f_p(0; \alpha)=0~~~~f_p'(0; \alpha)=1~~~~f_p'(\infty; \alpha) \to 0 \label{power_law_sak_bcs}
\end{align} \label{power_law_ode_general} \end{subequations} The associated streamwise velocity component, $u_x$, may be extracted as \begin{equation} u_x = U \frac{\partial f_p}{\partial \eta}. \label{streamwise_velocity_expression} \end{equation}The dimensional expression for the wall shear rate for a power law fluid, $\tau_p$, is likewise extracted from the solution for $f_p$, and is expressed as \begin{subequations} \begin{equation}
    \tau_p = K \left(U \left|f_p''(0; \alpha)\right|\left(\dfrac{\rho U^{2-\alpha}}{Kx} \right)^{1/(\alpha+1)}  \right)^\alpha \label{pl_dimensional_wall_shear}
\end{equation} In (\ref{pl_dimensional_wall_shear}), note that  $f_p''(0; \alpha)<0$ for the Sakiadis flow, and $f_p"(0; \alpha)>0$ in the Blasius flow. The analagous expressions for a Newtonian fluid can be obtained from (\ref{pl_dimensional_wall_shear}) by inserting $\alpha=1$ and $K=\mu_0$ in accordance with (1) to yield \begin{equation}
    \tau_n = |f_n''(0)| \sqrt{\dfrac{\rho \mu U^3}{x}} \label{newtonian_dimensional_wall_shear}
\end{equation} \end{subequations} where the subscript n denotes the Newtonian wall shear rate, and $f_n(\eta) = f_p(\eta; 1)$.

\subsection{Carreau Fluid}

\hspace{\parindent} The Boundary-Layer equations for a Carreau fluid do not admit a self-similar solution as for Newtonian and Power-Law Fluids. In general, this necessitates a numerical solution of the governing PDE system (5) (using $\mu$ in (2)) for Blasius or Sakiadis configurations. However, following the approach of \cite{Griffiths}, one can transform the governing system into an ODE via the similarity variable used for a Newtonian fluid.  In this form, the $x$-dependence does not disappear completely, but arises solely as a parameter in the problem.  This necessitates that the ODE be solved at each streamwise location $x$.

As such, we define a dimensional stream function for the Carreau model, $f_c$, as\begin{equation} f_c = \psi \sqrt{\dfrac{\rho}{\mu_0 x U}}\end{equation} and the similarity coordinate \begin{equation} \eta = y \sqrt{\dfrac{\rho U}{\mu_0 x}}.\end{equation} Substituting (10) and (11) into (5), one obtains not a single ODE as in the self-similar case, but an ODE with an $x$-dependent parameter. If we define the local parameter $\delta$ as \begin{equation}
    \delta = \frac{\lambda^2 U^3 \rho}{\mu_0 x}, \label{delta_definition}
\end{equation}  \begin{subequations} and write \begin{equation} \bar{\mu}_\infty = \mu_\infty / \mu_0\end{equation} $f_c$ satisfies
    \begin{align}
        \left\{\bar{\mu}_\infty + (1-\bar{\mu}_\infty)\left[1+\alpha\delta \left(\frac{\mathrm{d}^2f_c}{\mathrm{d}\eta^2}\right)^2 \right] \left[1+\delta \left(\frac{\mathrm{d}^2f_c}{\mathrm{d}\eta^2}\right)^2 \right]^{(\alpha-3)/2} \right\}\frac{\mathrm{d}^3f_c}{\mathrm{d}\eta^3} + \frac{1}{2}f_c \frac{\mathrm{d}^2f_c}{\mathrm{d}\eta^2} = 0 \label{carreau_ode_form_ode} &\\ (\mathrm{Blasius})~~~~f_c(0; \alpha)=0~~~~f_c'(0; \alpha)=0~~~~f_c'(\infty; \alpha) \to 1  \label{carreau_ode_form_blasius_bcs} & \\
    (\mathrm{Sakiadis})~~~~f_c(0; \alpha)=0~~~~f_c'(0; \alpha)=1~~~~f_c'(\infty; \alpha) \to 0 \label{carreau_ode_form_sakiadis_bcs} &
    \end{align} \label{carreau_ode_form_full_system}
\end{subequations}

For given rheological parameters, the ODE system (\ref{carreau_ode_form_full_system}) has a single parameter, $\delta$, that changes with distance $x$ downstream (Figure 2). Thus, by solving the system (\ref{carreau_ode_form_full_system}) for an appropriate array of $\delta$ values that span the desired $x$-domain of interest, the whole flow field may be determined without considerable computational effort. As indicated in (12), the $x$-dependent parameter $\delta$ incorporates the constant $\lambda$ present in the Carreau viscosity model (2).  It is apparent from the location of $\delta$ in (\ref{carreau_ode_form_ode}) that it is an effective relaxation time---the inverse relationship with $x$ indicates that the magnitude of shear-dependent behavior decreases downstream.  This observation is intuitively pleasing, as it is consistent with the physics of a growing boundary layer in the original $x$-$y$ domain (Figure 2), and indicates that velocity gradients decrease similarly---thus reducing the amount of shear behavior.

As discussed in Section 1, it often the case that $\mu_\infty$ is not reached in practice. Although the approach to follow does not require $\bar{\mu}_\infty=0$, we neglect it for purposes of clarity in presentation. For $\mu_\infty=0$, the dimensional expression for the wall shear for the Carreau model is \begin{equation}
    \tau_{c} = \sqrt{\dfrac{\rho  \mu_0U^3}{ x}}f_c''\left(0; \delta\right)\left(1 + \delta f_c''\left(0; \delta\right)^2  \right)^{(\alpha-1)/2} .\label{carreau_wall_shear_dimensional_expression}
\end{equation}

To solve the relevant boundary value problems for Power Law (and therefore Newtonian for $\alpha=1$) or Carreau rheologies from Sections 2.2 and 2.3, we convert the relevant boundary value problems (\ref{power_law_ode_general}) and (\ref{carreau_ode_form_full_system}) into initial value problems by imposing a value of $f_c''(0, delta)$.  We then truncate the domain to a length $L$, and use RK4 with shooting to meet the $y\to\infty$ boundary conditions in (13c)---now imposed at $y=L$. The domain length is then increased and the calculation repeated until convergence is reached to within an absolute error tolerance of $10^{-6}$.

\section{New Sakiadis Solutions for Power-Law Fluids}

\hspace{\parindent} To facilitate the comparison with the Carreau model solution for Blasius and Sakiadis flows, the self-similar solution to the ODE (\ref{power_law_ode_general}) for a power law fluid is necessary. Power law solutions for Blasius flows are well-known for all values of $\alpha>0$. For the case of a Sakiadis flow, however, \cite{Fox} and \cite{NonNewtonian} indicated that no solutions exist for two distinct classes of power-law fluids: those with $\alpha < 1/2$ and $\alpha > 1$. This conclusion is based on an analysis of one particular asymptotic form of the Sakiadis flow away from the wall. In what follows, we show that alternative asymptotic forms are possible, and this enables power-law solutions to be obtained in these previously excluded regimes of $\alpha$.

\subsection{Shear-Thinning Fluids ($\alpha < 1/2$)}


We begin with a discussion of solutions for shear-thinning fluids.  In the Blasius case, it is known that there is a solution to the power-law Blasius equation for all $\alpha < 1$ with the property that $f \sim \eta$ as $\eta \to \infty$. In the Sakiadis case, however, the existence of a power law solution is only known for $\alpha \in (1/2, 1]$.  Considering the similarities between the ODEs governing Blasius and Sakiadis flows, this inability to obtain a power law solution outside of this range is not anticipated.  We resolve this non-analogous behavior in what follows.

For a Sakadis flow,  the dimensionless steamfunction $f_p(\eta; \alpha)$, satisfies (\ref{power_law_ode_only}) with boundary condition (\ref{power_law_sak_bcs}).The Newtonian Sakiadis equation ($\alpha=1$) admits a solution having asymptotic behavior that can be derived assuming $f_p(\eta; 1) \to C$ and using the method of dominant balance \begin{equation} f_p(\eta; 1) \sim C + Ge^{-C\eta/2} ~~ (\eta \to \infty)\end{equation} with $C$ and $G$ constants \cite{GenMeth}. For fluids that shear-thin with $1/2 < \alpha < 1$ an analogous dominant balance subject to the same assumption yields \begin{equation}
    f_p\left(\eta; \frac{1}{2} < \alpha < 1\right) \sim C + D\left(1 + E\eta \right)^{^{\frac{2\alpha-1}{\alpha-1}}} ~~ (\eta \to \infty), \label{sakiadis_shear_thinning_classical_asymptotic}
\end{equation} with $C, D$ and $E$ constants; as is standard for dominant balance problems, these constants remain arbitrary when only the asymptotic behavior is assessed (note that here, the constant $C$ is dependent on the value of alpha, but as written, the form of $f_p \sim C$ is the same for Newtonian and power law cases). Note that this behavior breaks the dominant-balance assumption for $\alpha < 1/2$, when the leading constant term $C$ becomes asymptotically subdominant to the second term, which approaches infinity as $\eta \to \infty$ for $\alpha < 1/2$. The conclusion that there was is solution for $\alpha<1/2$  in \cite{Fox} and \cite{NonNewtonian} is based on the asymptotic form (\ref{sakiadis_shear_thinning_classical_asymptotic}); in what follows, we show there is another dominant balance that may be satisfied that leads to an expanded range of $\alpha$ values.

In particular, the boundary condition (\ref{power_law_sak_bcs}) does not require \textit{a priori} that $f_p \sim C$ as $\eta \to \infty$. Rather, a solution to equation (\ref{power_law_ode_only}) that satisfies equation (\ref{power_law_sak_bcs}) admits the asymptotic form \begin{subequations} \begin{equation}
    f_p\left(\eta; 0 < \alpha < \dfrac{1}{2} \right) \sim H \left(\eta+J\right)^{p} ~~ (\eta \to \infty),
\end{equation} where \begin{equation} p = \dfrac{1-2\alpha}{2-\alpha} \end{equation}  and \begin{equation}
    H = \left(\dfrac{\alpha(\alpha+1)(2-p)}{p^{1-\alpha}(1-p)^{1-\alpha}}\right)^{^{\frac{1}{2-\alpha}}}.
\end{equation} \label{power_law_sakiadis_new_asymptotic} \end{subequations} \noindent In (\ref{power_law_sakiadis_new_asymptotic}), $J$ is an arbitrary constant that reflects the autonomous nature of the ODE (\ref{power_law_ode_general}).  Indeed, one may write $f_p''(0)=\kappa$ and vary the initial condition $\kappa$ to examine $f'$ as $\eta \to 0$. In so doing, we find that for $\kappa$ less than a critical value of $\kappa=\kappa_p$, solutions for $f_p$ are monotonically increasing for all $\eta \geq 0$, while they are nonmonotone and eventually decrease with $\eta$ for $\kappa<\kappa_p$. Figure 3, indicates, then, that the solution corresponding to $\kappa=\kappa_p$ is a separatrix solution delineating the two asymptotic behaviors. At $\kappa=\kappa_p$, the solution smoothly limits to $f_p' \to 0$ as $\eta \to \infty$. This value of $\kappa_p$ is, in fact, the only solution that has this property, and we propose it represents the correct solution for $\alpha<1/2$. 

\begin{figure}
    \centering
    \includegraphics[width=0.5\linewidth]{thick_best.png}
    \caption{Caption}
    \label{fig:placeholder}
\end{figure}

The separatrix solution is found numerically to match the asymptotic behavior (\ref{power_law_sakiadis_new_asymptotic}). Furthermore, numerical results confirm that the solution to the power-law Sakiadis equation has the asymptotic form (\ref{power_law_sakiadis_new_asymptotic}), and yields a computed value of $\kappa_p$ that closely follows the high-shear limit for wall shear observed in the Carreau model to be described later. Taken together, the consistency of the wall shear rate and asymptotic behavior predictions with Carreau predictions justify the conclusion that a new admissible Sakaidis power law solution has been found for $\alpha<1/2$.


\begin{figure}
    \centering
    \includegraphics[width=\textwidth]{pl_blas_kappa.png}
     \includegraphics[width=\textwidth]{pl_sak_kappa.png}
    \caption{$\kappa$, the nondimensional power law wall shear, as a function of $\alpha$ for the Sakiadis Boundary Layer (top) and Blasius Boundary Layer (bottom).}
    \label{fig:kappavsalpha}
\end{figure}

% The case $\alpha = 0$ is physically nonsensical, so this treatment gives the structure of all shear-thinning Carreau fluids except in the case that $\alpha = 1/2$. In this case, we conjecture that \begin{equation} f_p\left(\eta; \dfrac{1}{2}\right) \sim D \log(\eta) ~~ (\eta \to \infty), \end{equation} This is a natural guess because for $\alpha > 1/2$, $f$ goes to a constant asymptotically, and for $\alpha < 1/2$, $f$ goes to a small power asymptotically, meaning $f$ likely limits to a function that grows slower than any power but increases faster than a constant. While this is far from a complete demonstration, we find it likely that $f_p(\eta; 1/2)$ admits a logarithmic solution. 

\begin{figure}
    \centering
    \includegraphics[width=0.6\linewidth]{shooting_fixed.png}
    \caption{Shooting Showing Convergence to the separatrix solution with domain lengthening for shear-thinning Sakiadis with $\alpha < 1/2$. Plots in blue are above the separatrix orbit, and plots in green are below it, and thus obtain a local maximum in $\mathbb{R}^+$. }
    \label{fig:shooting}
\end{figure}


\subsection{Shear-Thickening Fluids ($\alpha > 1$)}

\hspace{\parindent} When $f_p \to C$ is also assumed for shear thickening ($\alpha>1$) power-law fluids, the asymptotic equation (\ref{sakiadis_shear_thinning_classical_asymptotic}) arises, and thus indicates that no solution satisfies the dominant balance, since the second term is not subdominant to the first \cite{Bender}. However, in this case we once again find that there exists another class of power-law solutions. Insight can be gained in this case by analogy to the well-known structure of the Blasius problem. For a power-law fluid in the Blasius boundary layer, it is known that the dimensionless streamfunction admits a ``singular solution," which is $C^4$ but not $C^5$ (four but not five times continuously differentiable) at a distinguished ``singular" point along the solution curve \cite{Zhizhin1978-us, Pavlov1982-qh}. We note that, unlike the shear-thinning case, the underlying ODE (\ref{power_law_ode_only}) is not Lipschitz for shear-thinning fluids, meaning that the usual theorem proving the uniqueness of a smooth solution (the so-called Picard-Lindelof Theorem) does not apply. 

For this flow, it is found that there exist constants $C_1$ and $C_2$ such that the solution satisfies $f_p(\eta; \alpha) = \eta + C_2$ for all $\eta \geq C_1$. Thus, according to equation (8), the streamwise velocity $u_x$ is plug (no velocity variation with increasing distance from the wall) for $\eta \geq C_1$. For $\eta<C_1$, however, the solution to the boundary layer equation (\ref{power_law_ode_general}) applies in its full form. At $\eta=C_1$, the fluid flow has zero velocity, zero shear, and has a continuous gradient of shear satisfying (\ref{power_law_ode_only}), but the 4th derivative in $f_p$ is discontinuous. Because of the scaling properties of the Blasius equation, choosing any $f_p''(0)=\kappa$ and solving (\ref{power_law_ode_only}) as an IVP will still yield this structure. 

We claim the situation for the Sakiadis boundary layer is analogous to that of the Blasius case; to our knowledge, this is the first time that this structure has been proposed for the Sakiadis problem. That is, a ``finite-width" solution to the power-law boundary-layer equation exists, having the property that outside of some bounding region above the plate, the fluid flow is exactly zero. Near the plate, the boundary layer behaves according to the solution of (\ref{power_law_ode_general}); at the edge of the boundary layer, there is a discontinuous 4th derivative in $f_p$, but the velocity, its gradient, and velocity profile curvatures are continuous and satisfy (\ref{power_law_ode_only}). Similarly to the shear-thinning case, numerical results indicate a separatrix solution exists between monotone and nonmonotone solutions, yielding a plot analogous in structure to Figure 3; here, however, the stream function becomes a constant for a finite value of $\eta$ (as slowly going to infinity as happens for $\alpha<1/2$) for $\kappa=\kappa_p$. This structure gives, it seems, the unique $C^3$ solution to the equation (\ref{power_law_ode_general}) for $\alpha > 1$ and yields values of $\kappa_p$ that agree with the high-shear limit in the Carreau model. 


\section{Power-Law/Newtonian models vs. Carreau Prediction for Wall Shear}

\subsection{Direct Comparison of Wall Shear}

We compare wall shear rate predictions for Newtonian, power law, and carreau fluids, in dimensionless form.  To do so, we express these shear expressions ((\ref{pl_dimensional_wall_shear}), (\ref{newtonian_dimensional_wall_shear}), and (\ref{carreau_wall_shear_dimensional_expression})) in dimensionless form using Newtonian scalings. Denoting dimensionless quantities with an overbar, we write \begin{equation}
    \bar{\tau}_i = \dfrac{\tau_i}{\rho U^2} \hphantom{\sum\sum} \mathrm{for}~~i=n, c, p
\end{equation} For a Newtonian fluid with viscosity $\mu_0$, we have that \begin{equation}
    \bar{\tau}_n = \kappa_n \operatorname{Re}_{n, x}^{-1/2}, ~~~~\kappa_n = f_p''(0; 1), \label{newtwsexpnondim}
\end{equation} where the local Reynolds number is given by \begin{equation} \operatorname{Re}_{n, x} = \rho xU/\mu_0. \label{newtonian_reynolds_number}\end{equation} 

For a power law fluid, the dimensionless wall-shear is expressed as \begin{equation}
    \bar{\tau}_p = \kappa_p^\alpha \operatorname{De}^{(\alpha-1)/(\alpha+1)} \operatorname{Re}_{n, x}^{-\alpha/(\alpha+1)}, ~~~~ \kappa_p = f_p''(0; \alpha),  \label{plwsexpnondim}
\end{equation} where $\operatorname{De}$ is the Deborah number given by \begin{equation}
    \operatorname{De} = \frac{\rho \lambda U^2}{\mu}.
\end{equation} In writing the expression (\ref{plwsexpnondim}), note that we have utilized the relation $K=\mu_0 \lambda^{\alpha-1}$ to relate the parameter $K$ and Carreau parameters.

Moreover, we have that the Newtonian scaling gives an expression for the nondimensional Carreau wall-shear is given by \begin{equation} \bar{\tau}_{c} = \kappa_{c} \operatorname{Re}_{n, x}^{-1/2} \left( 1 +  \kappa_c^2 \frac{\operatorname{De}^2}{\operatorname{Re}_{n, x}} \right)^{(\alpha-1)/2}, ~~~~ \kappa_c=f_c''(0; \delta), \label{carwsexpnondim}\end{equation} where $\operatorname{Re}_n$ is the local Newtonian Reynolds number as in (\ref{newtonian_reynolds_number}). 

Figure 5 shows a typical comparison of local wall shear predictions for Sakiadis and Blasius flows for shear thinning fluids.  Note that for small dimensionless distances down the wall (small $\operatorname{Re}_{n, x}$ in (\ref{newtonian_reynolds_number})), the power law model predictions agree with those of the Carreau model; for larger distances, the Newtonian model is in agreement.  As indicated, the power law and Newtonian predictions serve as asymptotes to the Carreau Curve. Analagous behavior for the shear-thickening case can be seen in Figure 6. 


\subsection{Comparison of Wall Shears for Different Models}

By the equations of section 4.1, we may directly compare the wall-shear expressions for different models, as a function of the Newtonian Reynolds number $\operatorname{Re}_{n, x}$ and nondimensional Deborah number $\operatorname{De}$. The generic behavior is that for low Reynolds number, near the front of the plate, the shear is high, and the wall-shear approaches the power-law limit. The wall-shear is itself in this case a power-law function of the Reynolds number as per (\ref{plwsexpnondim}), so this corresponds to a straight line on a log-log plot. For higher shears, further along the plate, meanwhile the fluid is approximately Newtonian, and the wall-shear approaches the Newtonian limit of scaling with Reynolds number to the $-1/2$ power as per (\ref{newtwsexpnondim}). The wall-shear for a Carreau fluid roughly follows the power-law behavior near the front of the plate, until the power-law line intersects the line from the Newtonian limit. For shears greater than this, the Carreau wall-shear is mostly the same as its associated Newtonian fluid. 

\begin{figure}
    \centering
    \includegraphics[width=0.4\linewidth]{wallshearsmall.png}
        \includegraphics[width=0.4\linewidth]{wallshearsmall.png}
    \includegraphics[width=0.4\linewidth]{blas_shear_1.png}
        \includegraphics[width=0.4\linewidth]{blas_shear_2.png}
    \caption{Wall Shear for Sakiadis (left) and Blasius (right) Boundary layers for different exponent $\alpha$ and Deborah Number $\operatorname{De}$.}
    \label{fig:wallshearfig}
\end{figure}

\subsection{Limiting Analyses}

One can see that in the shear-thinning case, for high values of rate of strain, we have that $\delta$ is small and so the expression (\ref{carwsexpnondim}) approaches (\ref{newtwsexpnondim}), shwoing very concretely that the wall-shear behavior of Carreau approaches that of a Newtonian fluid. Note that the behavior of (\ref{carwsexpnondim}) when $\delta$ is large enough to be in the power-law region is unclear. Physically, since the fluid behaves like a power-law fluid, one expects the result to approach (\ref{plwsexpnondim}), but this cannot be easily found from the present nondimensionalization without rescaling the Carreau model in accordance with the power-law regime. However, numerical results clearly indicate that (\ref{carwsexpnondim}) does behave in accordance with the power-law model in the large $\delta$ regime.



Indeed, for an arbitrary Carreau fluid, we note that sufficiently far along the wall (where the shear is low), the non-self-similar Carreau wall-shear is in good agreement with the self-similar prediction for a corresponding Newtonian fluid. For points close to the front of the wall, in the power-law regime, the Carreau wall-shear is also quite well-approximated by the predictions of a power-law fluid.

The transition region between these behaviors for the wall-shear is found to happen where the relevant wall-shear curves intersect. Because those power-law and Newtonian wall-shear curves require no non-self-similarity, this means we can also compute directly which regime is relevant to a particular point along the flow of a Carreau fluid using only intersection information from self-similar flows, without actually solving the method of lines system for the Carreau streamfunction. By setting (\ref{plwsexpnondim}) equal to (\ref{newtwsexpnondim}), this cutoff is found to happen at a critical value of $\delta$, called $\delta^*$, which is related to $\kappa_n$ and $\kappa_p$ (which, recall, are functions of $\alpha$) in a straightforward way as \begin{equation} \delta^* = \left(\dfrac{\kappa_p^\alpha}{\kappa_n}\right)^{2\frac{1+\alpha}{1-\alpha}}.\end{equation}

We note that $\kappa_p$ varies with $\alpha$ in a way that we tabulate in Figure (\ref{fig:kappavsalpha}). Over the entire range of $\alpha$ from $0$ to $2$, $\delta_*$ itself only changes from approximately 14 to 20, meaning that the critical value of $\delta$ is of order one when the cutoff between the power-law and Newtonian regimes occurs. This has the following physical interpretation. The applied shear in the boundary layer scales as the wall speed, $U$, divided by the characteristic boundary-layer width, which in the Newtonian case goes as $\sqrt{\nu_0x/U}$. As such, the applied shear goes like $U/\sqrt{\nu_0x/U}$. The equation $\delta \sim 1$ can be re-arranged to give us that \begin{equation} \dfrac{U}{\sqrt{\nu_0x/U}} \sim \dfrac{1}{\lambda}. \end{equation} So, to order-of-magnitude, the cutoff between the Newtonian and power-law regions happens when the applied shear is on the order of the reciprocal relaxation time $\frac{1}{\lambda}$, which is consistent with the typical interpretation of $\lambda $ in the Carreau viscosity model. However, since we can compute the dimensionless value $\kappa_p$, we also know with a much greater degree of precision where the cutoff occurs along the boundary layer for any particular $\alpha$. 

We note also that this finding strongly confirms the existence and physical practicability of the new solutions for the Sakiadis power-law for the cases $\alpha < 1/2$ and $\alpha > 1$. In both of these cases, one may note that the wall-shear plot approaches a power-law limit in the high-shear regime, and the value of $\kappa_p$ in those cases (which determines the intercept of the power-law limit on the log-log plot), seems to numerically match the correct limit of the power-law in this case. 

\begin{figure}
    \centering
    \includegraphics[width=0.45\linewidth]{kappa_car.png}
    \caption{Kappa for the Carreau Problem (left)}
    \label{fig:kappacar}
\end{figure}

As an application of this result, we point out that, for instance, a good approximation can be made to integrated versions of the wall shear for the Carreau viscosity model, which is not self-similar, by using the correct self-similar model in their appropriate regimes. For example, suppose that one wanted to know the drag on the plate exerted by the boundary layer over some large length of plate. This may be obtained exactly by integrating the expression (\ref{carreau_wall_shear_dimensional_expression}) in dimensional form over the entire domain. Alternatively, using the known value of $\delta^*$ that corresponds to the known $\alpha$ for the Carreau fluid, we can deduce a critical $x^*$ in closed form before which the shear is approximately power-law and after which it is approximately Newtonian. Since the shear in both of those cases scales as a power of $x$, breaking the integral of $\tau_c$ up into pieces correctly yields an accurate estimate of the drag on the plate, with essentially no computational work besides the lookup of $\kappa_p$, for the much more difficult non-self-similar Carreau Sakiadis or Blasius Boundary Layer. 

\begin{figure}
    \centering \ContinuedFloat
    \includegraphics[width=0.8\linewidth]{prof1.png}

    \caption{Velocity Profile for the Self-similar and otherwise viscosity models for the case of a typical shear-thinning fluid an intermediate distance along the wall.}
    \label{fig:profiles}
\end{figure}


\begin{figure}
    \centering \ContinuedFloat
    \includegraphics[width=0.8\linewidth]{prof3.png}
    \includegraphics[width=0.8\linewidth]{prof4.png}

    \caption{Velocity Profiles for the Self-similar and otherwise viscosity models for the case of a fluid with much shear thinning for the Sakiadis problem at two distances along the wall. }
    \label{fig:profiles2}
\end{figure}

\begin{figure}
    \centering \ContinuedFloat
    \includegraphics[width=0.4\linewidth]{prof5.png}
        \includegraphics[width=0.4\linewidth]{blas_thick_30 - Copy.png}

    \includegraphics[width=0.4\linewidth]{prof6.png}
        \includegraphics[width=0.4\linewidth]{blas_thick_5 - Copy.png}


    \caption{Velocity Profiles for the Self-similar and otherwise viscosity models for the Sakiadis Problem in the shear-thickening case (left) and the Blasius problem (right) at two distances along the wall}
    \label{fig:profiles3}
\end{figure}

\section{Discussion}

\subsection{Approximation Accuracy for Wall Shear}

One may note that the expression (\ref{carwsexpnondim}) does not limit especially cleanly to (\ref{plwsexpnondim}) in the limit that $\operatorname{Re}_{n, x} \to 0$. In principle, this limit is difficult to analyze simply because we chose the Newtonian scaling for the Carreau fluid, which is not well-suited to the front-of-wall limit. This leaves for examination, however, the question of the nature of the approximation (\ref{plwsexpnondim}) to (\ref{carwsexpnondim}).

Note that the parameter $\kappa_c$ in the solution to (\ref{carwsexpnondim}) contains the full solution to the BVP (\ref{carreau_ode_form_full_system}), and varies to fix the Newtonian scaling to, intuitively, the appropriate power-law behavior in the $\operatorname{Re}_{n, x} \to 0$ limit. Furthermore, physically, one should expect this approximation to the wall shear to be quite good, because very near the front of the wall, the power-law boundary-layer thickness is much smaller (in relative terms) than the Newtonian power-law thickness. Furthermore, the shear in this region is quite high, so the fluid behaves rheologically much like a power-law fluid in this regime. Since on the scale of the Newtonian influence, one expects power-law behavior to be quite flat not very far up from the wall near the front of the wall, it follows that the Newtonian region is intuitively unlikely to have too much of an influence in that regime, since, very basically, not very much is happening in the fluid in the Newtonian region anyhow. 

\begin{figure}
    \centering
    \includegraphics[width=\linewidth]{ws_pt_3_sak.png}
    \includegraphics[width=\linewidth]{ws_pt_7_sak.png}
    \caption{Error Behavior of Wall Shear Predictions Near the Front for the Wall for $\alpha = .3$ and $\alpha = .7$ for various Deborah number. The power-law fluid produces Carreau-matching asymptotic form with a slight error in the asymptotic coefficient that decreases as $\alpha \to 1$.} 
    \label{fig:ws_front_err_proof}
\end{figure}

As a consequence of this intuition, it is very natural to make the guess that we should have $$ \bar{\tau}_c \sim \bar{\tau}_p ~~(\operatorname{Re}_{n, x} \to 0).$$ However, this claim is \textit{false}. A priori, because the solution is to the relevant BVP (\ref{carreau_ode_form_full_system}), there is no requirement the approximation $\bar{\tau}_c \approx \bar{\tau}_p$ meet precise solid asymptotic condition in $\operatorname{Re}_{n, x}.$

In fact, numerically, with quite a lot of genericity, it appears that $\bar{\tau}_p$ reproduces the correct asymptotic form for $\bar{\tau}_c$ near the front of the wall, but fails to reproduce the correct asymptotic constant. Indeed, the correction of the asymptotic constant, which---since the rheology is asymptotically Power-law in this region, must be due to downstream influence from the Newtonian region of the velocity profile---seems to be required, monotonically decreasing as $\alpha \to 1$, and, most surprisingly, independent of Deborah number (this is evidenced by Figure (\ref{fig:ws_front_err_proof})). That is, there exists a correction function $c(\alpha)$ with $c(1) = 0$, having the property $$ \bar{\tau}_c \sim (1 + c(\alpha)) \bar{\tau}_p ~~ 
(\operatorname{Re}_{n, x} \to 0).$$

We note that this is consistent with the key identity of the Polhausen Approach to Wall-Shear computation, which shows Wall-shear as a global integral of properties of the velocity profile. Indeed, we have in the Blasius case, say, that \begin{equation}
    \bar{\tau}_i = \frac{\text{d}}{\text{d}x} \int_0^\infty u_x(U-u_x) ~ \text{d}z \label{schlic} \end{equation}

Thus, the global velocity field affects the felt wall-shear. Note that, even in the low $\operatorname{Re}_{n, x}$ regime, near the front of the plate, the behavior of the Carreau fluid is Newtonian far enough from the wall (for $z$ large). Hence, the result (\ref{schlic}) indicates that the behavior of $\tau$ may fail to be completely captured by the power-law limit even though that describes most of the flow field, which expresses itself in the asymptotic error $c(\alpha)$. 

For practical fluids, by the (again, empirically observed) monotonicity of $c$ in $\alpha$, the worst case scenario error incurred is reached for the lower-range of practical shear-thinning, when $\alpha \sim .3$. In this regime we have $c$ is on the order of $.1$, and so the error incurred is in the worst case about $10\%$. In practice, this is not much more in absolute or relative terms than the error incurred from the transition-region near where the rheology switches from Newtonian to power-law. In the Newtonian region, it appears we do have true asymptotic matching between $\tau_c$ and $\tau_n$ as $\operatorname{Re}_{n, x} \to \infty$, so this worst-case error is still in practice not such a problem. For most practical settings, the self-similar solutions are perfectly well-predictive, while for more accurracy one can still solve the one-parameter family of ODEs (\ref{carreau_ode_form_full_system}) to obtain the full solution for a Carreau fluid. 

\section{Discussion}

A plot of the properly compared wall shears as a function of $\operatorname{Re}_x$, which scales with the distance along the plate $x$, has the intuitive transition structure described above and is shown in Figure (\ref{fig:wallshearfig}). We note how this behavior maps on the underlying rheology quite cleanly. At a physical level, this may imply that it is the relevant viscosity at the wall, more than it is the impact of shear-thinning on the velocity profile, that affects the wall shear rate for the Sakiadis and Blasius problems. This does suggest that capturing the wall-shear behavior does not necessarily mean that the full fluid profile is correctly captured, therefore. Analysis of the profiles shows that there are many instances in which the wall-shear prediction is qualitatively good even when the entire velocity profile is not, since the correct gradient only needs to be captured in the vicinity of the wall (though still in a boundary value problem). 

We note that, while the power-law model provides accurate description of the wall-shear of a Carreau fluid in the high-shear regime (that is, near the front of the plate in the wallwise direction), this does not necessarily mean that the entirety of the power-law predicted velocity profile is accurate for $\eta$ large (that is, far away from the plate in the direction perpendicular to the wall). After all, the shear prediction requires only good resolution of the velocity profile in the vicinity of the wall---albeit slightly complicated by the fact that the velocity solution is the solution to a boundary value problem, so the far-from-the-wall behavior does have some effect.

The profiles shown indicate that the velocity profile from the power-law model is not a perfect estimate for the Carreau fluid even for some values of $\delta$ for which the wall-shear prediction of the associated power-law fluid matches the Carreau wall-shear. That said, too far up from the plate very close to the front of the plate, we note that the fluid streamlines in the Sakiadis and Blasius solutions do not appear to have small-slope. Thus, the boundary-layer assumptions are breaking down in the regime in which the power-law solution does not give accurate descriptions of the Carreau velocity field. While there still does appear to be a region in which the Carreau velocity field is not well-matched by the power-law while the boundary-layer approximation holds, we note that the full Carreau solutions appears to give physically meaningful behavior in that case, and can still be computed directly. 

We note that the match of wall-shears even in regions where the velocity profile matches less well implies that it is the behavior of the non-Newtonian viscosity $\mu$, rather than the impact of the changed profile on the applied shear $\partial u_x/\partial y$, that has the greater effect on the wall-shear of a Carreau boundary layer. We note that this is the \textit{opposite} of the effect that non-Newtonian rheologies have on the stability of laminar boundary-layers according to \cite{Griffiths}, which is of some interest, though of course not particularly surprising given that stability and shear rate do correspond to rather different features of the flow field. 

We also point out that the fact that the velocity profiles from a power-law fluid cannot be taken too directly to match the behavior of real systems far from the plate, is pleasing in conjunction with the fact that this is precisely the regime in which the singular behavior of the singular solutions take effect. In some sense, this indicates that one really doesn't need to be too careful in description of the flow far from the plate, since the flow is essentially plug, but that if one does want very precise, quantitative estimates for the full velcoity profile of a non-Newtonian fluid flowing in a boundary layer, the precise rheological effects of the Newtonian plateau is significant. 

The Carreau solution to these problems serves as a significant validation to the physics of the power-law---and, to a lesser extent even the Newtonian, as a good approximation to a wider-class of fluids that don't shear-thin or thicken too much---boundary layer flows. 

We point out the work of Denier and Dabrowski \cite{Dabrowski}, who considered in the Sakiadis case the origin of the breakdown in the solution to the full Boundary-layer problem for shear-thickening fluids. They point out that this can be seen as a breakdown in the boundary-layer equations. Note that for a shear-thickening fluid, far from the wall, there is little shear, and pure-power law behavior (in the shear-thickening case), predicts the fluid to be approximately invsicid in that regime. Because the Carreau solution is perfectly well-behaved far from the wall (though, admittedly, the boundary-layer approximation is technically invalid there), this seems to be because the fluid is not behaving sufficiently viscously to match physical behavior far from the wall for a shear-thickening power-law fluid. This is matched by Denier and Dabrowski's work, in which he points out that one can also recover a solution to this flow by throwing away the boundary-layer assumptions. Their analysis characterizes this solution as containing a ``viscous diffusion layer", where the effect of viscosity coming from shear not normal to the wall is relevant, and that allows one to recover a solution to the problem. We note that this also corresponds to a way to make the fluid behavior more viscous in that regime, and that in a somewhat more fundamental sense, correcting the problem of non-smoothness in the solution for shear-thickening fluids is about correcting a breakdown in the power-law rheology in the relevant low-shear regime. 

Finally, we note that this also validates the behavior of the power-law model in the shear-thickening case in a way similar to the shear-thinning one. One can generally achieve physically meaningful behavior for any regime with the Carreau model, and the power-law solution gives an especially good approximation to that for the wall-shear near the front of the plate, while also providing reasonably accurate, though sometimes imperfect far-from-the-wall velocity profiles. 

\section{Conclusion}

The present work investigated the boundary-layer flows of a non-Newtonian fluid over a flat plate, focusing on both the Sakiadis and Blasius boundary layers. While the mostly realistic Carreau viscosity model does not admit self-similar solutions, the wall-shear predictions in the flow are well approximated by the self-similar solutions in the Newtonian and power-law regimes. These regimes have been identified a priori of Carreau calculations, and were found to match the Carreau flow. The self-similar solutions provided reliable wall-shear predictions in each regime, and this framework was applicable for both the Sakiadis and Blasius flows across a wide range of rheologies. As part of this, for the Sakiadis flow, we showed that solutions do exist in a weak sense for certain power-law exponents, where previous work had suggested no solutions were possible, and that these solutions aligned with the wall-shear predictions of the power-law regime of the Carreau model. 


\bibliographystyle{plain} % We choose the "plain" reference style
\bibliography{biblio} % Entries are in the refs.bib file


\end{document}